User routes

/users
/users/:id
/login
/users/validate
/users/validate/:regCode
/photos
/photos/:id (Por req.body le pasamos la descripción (coincidencia de palabras))
/photos/(busqueda por descripción) //filter(element){
/photos/:id (delete)
    
}
/likes/:id (post, delete)

Entries routes

/entries
/entries/:id